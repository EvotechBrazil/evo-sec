# STATE — evo-sec (Nina)   (atualizado 2026-06-25 por Claude)

> ## 🔖 PARADO AQUI (2026-06-25) — RETOMAR
> **Sessão gigante (8 SPECs + premortem do sistema), tudo na `main` e validado E2E em prod (PRs #34–#44):** SPEC-004 alertas proativos · 005 cérebro multi-turno (memória) · 006 contexto durável · 007 data/timezone do orb · 008 hardening (resiliência+rate-limit) · 009 Onda 1b. **Premortem com 25 riscos:** `.ai/PREMORTEMS/PREMORTEM-sistema-2026-06-24.md`.
> **Release-blockers (Onda 1) — TODOS ✅:** #3 resiliência **✅** (backend + n8n: onError+fallback) · #2 backup **✅ diário 2h no EasyPanel (LOCAL — falta off-site + testar restore)** · #1 webhook HMAC **✅ FECHADO E VERIFICADO E2E (2026-06-25)** — nó `Valida Segredo (webhook)` (segredo via **query-param `?token=`**, pois Evolution 2.3.7 não tem header; env no **serviço n8n** + `N8N_BLOCK_ENV_ACCESS_IN_NODE=false`); forjado bloqueado (exec 827/828, parou no gate) + real passa ("pong"); `activeVersionId 8536d345`. Doc: `n8n/workflows/nina-webhook-seguranca.md`.
> **CONTINUAR (ordem):** (1) backup **off-site** (Backblaze B2 grátis → destino no EasyPanel) + testar restore num clone; (2) **Onda 2** do premortem: #6 lembrete recorrente (feature quebrada), #8 tz financeiro, #9 idempotência, #14 observabilidade. **(#1 webhook ✅ fechado 2026-06-25 — não é mais blocker.)**
> **Drift detectado:** o draft de timeouts da SPEC-009 (b) (`options.timeout=15000` nos httpRequest) **não está** no workflow vivo (a resiliência n8n no ar é o onError+fallback `Evolution sendText (fallback)`; o cap de timeout segue pendente na UI).
> **Como:** Claude dispara deploy (webhook do `api`, Tiago tem o token) + edita n8n via MCP; **Tiago publica n8n + configura EasyPanel/Evolution**. Modelo do brain WhatsApp = `qwen/qwen3.7-plus`.

## Status atual
**EM PRODUÇÃO no EasyPanel.** Sprints 1–4 concluídas + deploy completo + camada de tools do n8n (Nina persiste de verdade). Backend (GTD, Agenda, Financeiro, Finanças/coach, Custo), Dashboard (6 telas) e n8n rodando ponta a ponta, validados E2E por texto e áudio no WhatsApp.

> **2026-06-22 — Reset de dados p/ teste full:** bancos **dev** (local Docker `evosec-pg`, zerado + re-seedado) e **prod** (`nina` @ VPS/EasyPanel) limpos — 12 tabelas de dados truncadas (`RESTART IDENTITY CASCADE`). Em prod, `tenants/users/modelos/configs` **preservados** (login + roteamento WhatsApp + modelos intactos); schema/migrations mantidos. Login owner inalterado.

> **2026-06-23 — Sessão (PRs #28–#32, na `main`):** SPEC-003 financeiro **validado E2E em prod** (saldo R$100 sem dobra); visão/áudio → `gemini-2.5-flash` (o `gemini-2.0-flash-001` foi aposentado pelo OpenRouter); voz do orb `/falar` → **ElevenLabs** (igual WhatsApp); cérebro da API → `gemini-2.5-flash-lite` (orb rápido/barato, era ~19s no qwen); botão **excluir** conta no app; `max_tokens` 700→2000. **Tiago fez o deploy** (envs `ELEVENLABS_API_KEY` + `OPENROUTER_MODEL_INTER` setadas; API confirmada no ar via probe `/nina/voz`→401). **Falta conferir (próxima sessão):** (1) self-chat "o que tenho a pagar" → confirmar pelos dados a exclusão dos 2 títulos de teste (`e14fe684` R$250 "Entrada" / `64916a23` R$150 "Mão de obra CLASSIC Du Cavalaro"); (2) 1 áudio → confirmar transcrição `gemini-2.5-flash`; (3) orb no app (não logável) — Tiago confere voz feminina + velocidade. Opcional: fazer o fluxo responder "só leio imagem/PDF" p/ zip/doc não-visual (hoje dá 400 na visão, ex. exec 569 = um `.zip`).

> **2026-06-23 — Sessão (SPEC-004 alertas proativos):** Nina agora **proativa** — 3 alertas agendados: vencimentos (atrasadas/hoje/próximos), aporte/meta atrasada (com disclaimer), follow-up "aguardando" (GTD). Backend no `ResumoModule` (`GET /resumo/vencimentos|aportes|follow-ups`, envelope + flag `temAlerta`, opt-out por Config), 27 testes, 2 auditores OK → **PR #34 mergeado na main**. n8n: workflow **`b4gopjjyGKMrCJaP`** (`Nina — Alertas Proativos`, 9 nós) criado via MCP, 2 auditores OK; **Tiago publicou + ativou + atualizou cred do Evolution**. **Falta (Tiago):** (1) cred "Nina API service token" no nó `Buscar Alerta`; (2) **redeploy da API** (endpoints `/resumo/*` novos) — sem isso o cron dá 404; (3) validar E2E (n8n Execute / criar dado de teste). Detalhes: SPEC-004 abaixo.

### Produção (EasyPanel) — ✅ NO AR (2026-06-18)
Painel `https://easypanel.evotechsystem.cloud` (IP `72.61.57.251`, wildcard `*.rte6ms.easypanel.host`). Projeto **`nina`**, 3 serviços:
- **`db`** — Postgres 16. Host interno: `nina_db:5432`, database `nina`, user `postgres`.
- **`api`** — `https://nina-api.rte6ms.easypanel.host/api/v1`. Build por **Dockerfile** (`backend/Dockerfile` + `docker-entrypoint.sh` roda `prisma migrate deploy` + seed no boot). Source = repo público `main`, path `/backend`.
- **`frontend`** — `https://nina-web.rte6ms.easypanel.host`. Build por **Dockerfile** (`frontend/Dockerfile`, Next 14, `NEXT_PUBLIC_API_URL` embutido no build). Source `main`, path `/frontend`.
- Env/segredos no serviço `api` (JWT*, `SERVICE_TOKEN`, `ENCRYPTION_KEY` [32 chars exatos], `SEED_OWNER_PASSWORD`). Login owner: `rodrigo@crossfitarapongas.com.br`.
- **Deploy:** push no `main` + trigger (webhook `/api/deploy/<token>` ou redeploy no painel). API da EasyPanel é tRPC `POST /api/trpc/*` com body `{json:{...}}` (GET-com-input quebrado no servidor).
- **Pendências de produção:** rotacionar token da API EasyPanel + senha seed; RLS camada 2 (role não-owner, ADR-006); CORS restrito ao domínio do front.

### Workflow n8n vivo (2026-06-18) — ✅ ATIVO, PERSISTE NA API (texto + áudio)
`Nina — Principal (WhatsApp)`, id **`Dqm3pJo2MNHcRZ1R`**, instância `alicia-n8n.rte6ms.easypanel.host`, **21 nós**. Webhook produção `https://alicia-n8n.rte6ms.easypanel.host/webhook/nina`.
- **Fluxo:** Webhook → **Filtro de Gatilho** (self-chat + sessão, tolera 9º dígito BR) → **Nina (OpenRouter `qwen/qwen3.7-plus`)** devolve **JSON `{acao, dados, resposta}`** → **Interpreta Acao** (parse robusto) → **Roteia Acao** (switch) → nós HTTP **`API: criar recado/tarefa/lembrete/conta/compromisso`** → **Finaliza Resposta** → **Modo de Resposta** (texto `sendText` | áudio `Gerar Voz (ElevenLabs)`→base64→`sendWhatsAppAudio`).
- **10 ações:** criar recado/tarefa/lembrete/conta/agenda/meta + **pagar_conta, aportar_meta, cancelar_agenda** (act-on-entity, com **confirmação sim/não** via `pendingAction` em static data) + conversa. Visão/OCR (foto→Gemini→Nina).
- **Auth da API:** header `x-service-token` via credencial Header Auth **"Nina API service token"** (id `106lKMqNprmKFB1k`) + `x-tenant-id` literal (tenant `00000000-0000-0000-0000-000000000001`).
- **Áudio:** transcrição via **`google/gemini-2.5-flash`** (chat/completions, `input_audio` ogg). ⚠️ **OpenRouter NÃO transcreve o `ogg/opus` do WhatsApp via `/audio/transcriptions` + whisper (dá 404 "Provider returned 404"; whisper só aceita wav/mp3)** → por isso Gemini. Resposta em voz via **ElevenLabs** `eleven_multilingual_v2` (credencial Header Auth `xi-api-key`, ≠ Evolution `apikey`).
- **⚠️ Modelo multimodal trocado (2026-06-23):** o OpenRouter **aposentou `google/gemini-2.0-flash-001`** ("No endpoints found") → **foto e áudio quebraram** (exec 561 com erro). Nós **`Visao (Gemini)`** (image_url) e **`Transcrever (OpenRouter)`** (input_audio) migrados p/ **`google/gemini-2.5-flash`** (suporta image+audio) via MCP `setNodeParameter` + `publish_workflow` (versão ativa `913683e0`). Texto (`qwen`) nunca foi afetado.
- ⚠️ **MCP n8n** bloqueia criar/listar credenciais e anexar `httpHeaderAuth` via `addNode` → nós HTTP novos precisam ter a credencial selecionada na UI + **Publish**.
- **De-branding** mantido (sem citar empresas/marcas). Evolution: `https://alicia-evolution-api.rte6ms.easypanel.host`, instância `nina`.

### Cérebro na API + voz no app (2026-06-18, PR #20)
- **`POST /api/v1/nina/mensagem`** {texto, pendente?} (JWT, tenant-scoped) — `NinaModule` (`OpenRouterAdapter` + `NinaService` reusando os services; services agora `exports`). Mesma lógica do n8n (intenção qwen → executa criar_* ou pede confirmação em pagar/aportar/cancelar), com **confirmação stateless** (API devolve `pendente`; cliente reenvia no "sim"). Requer **`OPENROUTER_API_KEY`** no env (senão 503).
- **Voz ElevenLabs no app + fix resposta truncada (2026-06-23):** o orb `/falar` agora chama **`POST /nina/voz`** (novo, `ElevenLabsAdapter`) e toca a **MESMA voz do WhatsApp** (voice `gX4eTo1XOTTALJXnDro4`, `eleven_multilingual_v2`) — antes usava o TTS do navegador (voz do aparelho, vinha masculina). Fallback automático p/ navegador se a chave faltar/erro. Config via env `ELEVENLABS_API_KEY` (req.), `ELEVENLABS_VOICE_ID`/`ELEVENLABS_MODEL` (default = WhatsApp). Também subi `max_tokens` 700→2000 no `OpenRouterAdapter` (qwen3.7 gasta ~900 tokens em *reasoning* → o JSON vinha truncado/estranho na voz do app).
- **Orb mais rápido (2026-06-23):** cérebro da API (orb `/falar`) trocado de `qwen/qwen3.7-max` (modelo de raciocínio, ~19s/resposta) p/ **`google/gemini-2.5-flash-lite`** (rápido + ~9x mais barato: $0,10/$0,40 vs $1,25/$3,75 por M tokens). É o novo default de `OPENROUTER_MODEL_INTER`; em prod, setar a env torna definitivo (caso exista override antigo). WhatsApp (n8n) segue no qwen — alinhar é opcional.
- **Troca de senha:** `PATCH /auth/senha` (JWT) + tela `/configuracoes` + auto-logout em 401 (PRs #18/#19).

### Dashboard (2026-06-17/18) — tema mobile-first preto+amarelo, Archivo
7 telas + **`/falar` agora com VOZ REAL** (Web Speech API STT pt-BR + TTS, chips, fallback de texto, confirmação). UI kit `src/components/ui.tsx`.

## Roadmap restante (HANDOFF — retomar aqui)
**Manual (não dá por código):**
0. **NOVO (2026-06-23): `ELEVENLABS_API_KEY` no env do `api`** (EasyPanel) + redeploy → habilita a voz ElevenLabs no orb `/falar` (igual ao WhatsApp). Sem ela, o orb cai no TTS do navegador. `ELEVENLABS_VOICE_ID`/`ELEVENLABS_MODEL` já vêm com default = WhatsApp. **Recomendado também:** setar **`OPENROUTER_MODEL_INTER=google/gemini-2.5-flash-lite`** (orb rápido/barato; se não setar, o novo default do código já vale).
1. ~~**`OPENROUTER_API_KEY`** no env do serviço `api` (EasyPanel) → redeploy.~~ ✅ **RESOLVIDO (2026-06-19):** chave setada no env do `api` + **redeploy** → `/nina/mensagem` voltou (orb da voz no app responde 200). ⚠️ Gotcha: o adapter lê o env **no boot** (`private readonly env = loadEnv()`), então adicionar a env no EasyPanel **só vale após Deploy/Restart** do serviço.
2. **n8n — selecionar credencial + Publish** nos nós HTTP novos: `Listar metas`, `Aportar meta`, `Listar agenda`, `Cancelar compromisso`, `API: criar meta` → **Nina API service token**; `Baixar Midia` → **Evolution**; `Visao (Gemini)` → **OpenRouter account**.
3. **Validar:** voz `/falar` (Chrome); pagar/aportar/cancelar com confirmação (WhatsApp); visão/OCR (foto).
4. **Pré-prod:** rotacionar token EasyPanel + senha seed (via `/configuracoes`); **RLS camada 2** (ADR-006); CORS restrito; GO/NO-GO.

## Histórico
Sprints 1–4 (10 PRs): backend NestJS (auth dupla JWT + `x-service-token`, tenant, CRUD GTD, Agenda c/ recorrência, Financeiro, Finanças/coach, Custo), dashboard, n8n (prompts + filtro + multimodal). Deploy EasyPanel: PRs #16 (Dockerfile backend) e #17 (Dockerfile frontend).

## SPECs
- **SPEC-002** (implementada — validada E2E em produção) — Digest matinal + semanal da Nina. `GET /resumo/diario|semanal` tenant-scoped (`ResumoModule` reusa Recados/Tarefas/Lembretes/Agenda/Financeiro services + `ResumoRepository` p/ VIPs/Config/Tenant). Resposta envelopada `{data:{ativo,numero,dia|inicio/fim,resumo,texto}}` — texto pronto p/ WhatsApp (truncado 2k diário / 4k semanal, tz-aware America/Sao_Paulo, moeda em centavos). Opt-out via Config `digest_diario_ativo`/`digest_semanal_ativo` (ausente=ativo); número = `Tenant.whatsapp_number` → **sem migração**. Helpers `format.util` (sparkline/seta/fmt/tz) portados da skill `relatorio-diario` (Bravy). Workflow n8n documentado em `n8n/workflows/nina-digest.md` (Schedule diário `45 7 * * 1-5` / semanal `0 17 * * 5` → HTTP → IF ativo → Evolution sendText).
  - **Harness:** 22 testes unitários (helpers + service mockado + guarda de tenant no repo) + **E2E real** validado contra Postgres efêmero (vazio→"tudo em dia", populado→agenda/atrasado/vence-hoje, VIP-aguardando, opt-out→ativo:false, 401 sem auth, fuso e moeda corretos). Bug pego e corrigido no E2E: campo `data` colidia com o envelope do ResponseInterceptor → renomeado p/ `dia`.
  - **Workflow n8n `rob9zT99LztycoVp`** (`Nina — Digest Matinal + Semanal`) — 2 Schedule (diário `0 45 7 * * 1-5` / semanal `0 0 17 * * 5`) → HTTP GET `/resumo/*` (httpHeaderAuth + `x-tenant-id`) → IF (`data.ativo` true && `data.numero` notEmpty) → Evolution sendText. Credenciais já apontadas (UI) e workflow **ativo**. IF: o MCP normaliza `typeValidation` p/ `strict` → quebra boolean `is true`; corrigido p/ `loose` + `looseTypeValidation:true` via `setNodeParameter`. Duplicado de teste `4z8kXMEBttdr5kz3` arquivado.
  - **VALIDADO E2E EM PRODUÇÃO (2026-06-19):** execuções manuais `244` (diário) e `245` (semanal) = sucesso; **2 WhatsApps entregues** ao Rodrigo (`5543999864409`). API no ar, credencial OK, número preenchido, fuso/moeda/curva corretos.
  - **Polish (2026-06-19):** curva semanal agora começa no **domingo** (`limitesDaSemana`, label `DSTQQSS`); espaços sobrando removidos nos KPIs (trim por linha); `frontend/src/app/mockups` (v1–v9 órfãos) removido do build.
  - **Pendências (Tiago):** (1) **republicar/Salvar** o workflow n8n — o fix do IF está no rascunho, mas a versão ATIVA ainda é `strict`; sem republish o cron quebra (publish via MCP bloqueado pelo classificador); (2) **redeploy da API** p/ os ajustes de polish (curva domingo + trim) valerem em produção.

- **SPEC-003** (✅ **EM PRODUÇÃO — validado E2E no WhatsApp 2026-06-23**) — Módulo financeiro completo: a Nina entende **entrada/saída de caixa**, **contas a pagar/receber**, **baixa de título** e **saldo**; lançamento manual no app via REST (sem WhatsApp). **ADR-007**: `Conta` unificada via `origem AVULSO|TITULO` (avulso nasce quitado → saldo de fonte única, sem dobra); `Categoria` como tabela tenant-scoped com `grupoDre` (DRE); regime de caixa; confirmação desacoplada do canal.
  - **Migração** `20260622225327_spec_003_financeiro` (+`origem`, +`categoria_id`, tabela `categorias` com RLS `tenant_isolation`) + seed **19 categorias** (plano de contas enxuto, ref. skills-contadores).
  - **Backend:** `FinanceiroService.registrarMovimentacao/resumo/pendentes`; novos endpoints `POST /financeiro/movimentacoes`, `GET /financeiro/resumo` (saldo+DRE+breakdown), `GET /financeiro/pendentes`; módulo `categorias` (CRUD + `garantir-padrao` + `resolverPorNome`).
  - **Nina:** novas ações `registrar_movimentacao`, `consultar_saldo`, `consultar_contas`; `criar_conta`/`pagar_conta` agora com **confirmação por botões** (`confirmacao` no `NinaReply` — o canal renderiza: WhatsApp Evolution buttons / app `ConfirmCard`; labels mapeiam AFIRMA/NEGA/"recado"). Prompt do `OpenRouterAdapter` atualizado (regra: "entrou/saiu/paguei"=movimentação; "tenho que pagar/vou receber"=conta; "foi paga"=baixa).
  - **App:** `/financeiro` com **lançamento rápido** (entrada/saída + categoria → REST), **botão baixar** nas pendentes, KPIs do resumo e **donut por categoria** (tema preto+amarelo).
  - **Harness:** typecheck back+front verde; **4 testes unit** (saldo sem dobra); **smoke E2E REST real** (login→+250→−150→saldo 100, sem dobra; 19 categorias). Docs: `.ai/HARNESS_RESULTS/SPRINT-5-spec-003-financeiro.md`, premortem `.ai/PREMORTEMS/PREMORTEM-spec-003-financeiro.md`, SPEC `.ai/SPECS/SPEC-003-modulo-financeiro.md`.
  - **n8n WhatsApp (DRAFT pronto 2026-06-22):** workflow `Dqm3pJo2MNHcRZ1R` agora com 43 nós — prompt do nó `Nina (OpenRouter)` **sincronizado** (12 ações, regra entrou/saiu=movimentação) + switch **`Roteia Financeiro`** que intercepta o fallback (output 11) do `Roteia Acao` + nós `API: registrar movimentacao` (POST /financeiro/movimentacoes), `API: resumo` (GET /financeiro/resumo)→`Formata saldo`, `API: pendentes` (GET /financeiro/pendentes)→`Formata contas`. Fluxo religado com **zero validationWarnings**. As 11 rotas antigas e o `conversa` ficaram intactos. `registrar_movimentacao` executa direto (sem confirmação por botões — incremento futuro).
  - **✅ n8n no ar (2026-06-23):** credencial **"Nina API service token"** anexada nos 3 nós HTTP novos + **Publish** feito (Tiago). Workflow `Dqm3pJo2MNHcRZ1R` ativo, `versionId == activeVersionId`, 43 nós.
  - **✅ VALIDADO E2E EM PRODUÇÃO (2026-06-23):** self-chat "nina anota entrada de 250" / "saída de 150 mão de obra" / "qual meu saldo" / "o que tenho a pagar" → execuções **557–560 success**, os 3 nós financeiros responderam **200**. `API: resumo` (exec 559): entradas 25000, saídas 15000, **saldo 10000 (R$100) SEM dobra** (não soma pendentes — ADR-007 confirmado); classificação correta (não vira mais recado). Movimentações de hoje = `AVULSO` quitadas; os "em aberto" eram 2 `TITULO` PENDENTE criados em 2026-06-22 (dados de teste).
  - **App — botão Excluir (2026-06-23):** linha de cada conta no `/financeiro` agora tem **excluir** (com confirmação) → `DELETE /financeiro/contas/:id` (soft delete, backend já existia). Serve p/ gestão manual de títulos errados sem WhatsApp.
  - **Pendências (Tiago):** (1) **redeploy do `frontend`** (p/ o botão excluir) e então excluir os 2 títulos de teste pendentes — `e14fe684…` (A_RECEBER R$250 "Entrada") e `64916a23…` (A_PAGAR R$150 "Mão de obra CLASSIC Du Cavalaro"), ambos origem `TITULO`/PENDENTE de 2026-06-22; (2) opcional: **ConfirmCard**/botões Evolution (confirmação visual); (3) criar conta a pagar/receber com vencimento pelo app.
  - **Skills-contadores:** 57 skills ASV/Bravy em `docs/skills-contadores/` (referência, **gitignored** pela licença) + `.claude/skills/01..57` (comandos locais; 56 ativas, `01-simples-nacional` só na referência).

- **SPEC-004** (✅ **VALIDADO E2E EM PROD — 2026-06-24**) — **Alertas proativos**: a Nina dispara sozinha 3 alertas no WhatsApp. Backend no `ResumoModule` (espelho do digest SPEC-002), reusando services existentes + `ResumoRepository` + `format.util`; envelope `{ativo,numero,temAlerta,dia,resumo,texto}` (novo flag `temAlerta` → n8n só envia quando há o que avisar).
  - `GET /resumo/vencimentos` — contas atrasadas/vencem hoje/próximos 7d (separa A_PAGAR vs A_RECEBER), via `FinanceiroService.vencimentos()`. Opt-out `alerta_vencimentos_ativo`.
  - `GET /resumo/aportes` — metas atrasadas + aporte sugerido, via `FinancasService.evolucao()`. **Disclaimer educativo SEMPRE no texto** (guardrail do coach). Opt-out `alerta_aportes_ativo`.
  - `GET /resumo/follow-ups` — tarefas `AGUARDANDO` com `dataCobranca` vencida (verificação contínua GTD), via `TarefasService.list()`. Opt-out `alerta_aguardando_ativo`.
  - **Harness:** `tsc` verde; **27 testes** (tz-aware, opt-out por chave, `temAlerta` vazio, centavos, disclaimer, truncamento ≤2000). 2 auditores (segurança/tenant + QA) **APROVADO**. Docs: `.ai/SPECS/SPEC-004`, `.ai/PREMORTEMS/PREMORTEM-spec-004`, `.ai/HARNESS_RESULTS/SPRINT-6`. PRs: **#34** (backend) + docs n8n.
  - **n8n:** workflow `b4gopjjyGKMrCJaP` (`Nina — Alertas Proativos`, 9 nós: 3 Schedule → Set rota → HTTP GET → IF `ativo && temAlerta && numero` `loose` → Evolution sendText). Crons: vencimentos diário 8h, aportes seg 9h, follow-ups seg-sex 8h30. Doc `n8n/workflows/nina-alertas.md`. **Publicado+ativado pelo Tiago (2026-06-23)**, cred Evolution OK.
  - **✅ E2E em prod (2026-06-24):** deploy no ar + cred service-token apontada; conta de teste "água R$155 venceu 20/04" criada pela Nina → `GET /resumo/vencimentos` devolveu `temAlerta:true` (atrasadas:1, R$155, texto formatado) → **WhatsApp enviado ao dono** (exec n8n `707`, Evolution `PENDING`). Os 3 endpoints subiram no mesmo deploy.
  - **Pendências:** (1) **timezone** — instância n8n em `America/New_York` (UTC-4) → crons disparam 1h depois do pretendido (venc 9h / follow 9h30 / aporte seg 10h SP); fix = setar timezone dos 3 gatilhos p/ `America/Sao_Paulo` + republish (mesmo detalhe latente no digest). (2) **excluir a conta de teste** (senão alerta todo dia). (3) **Cérebro da Nina (WhatsApp) perde contexto multi-turno** — slot-filling de várias mensagens quebra (confunde "conta atrasada/em aberto" com "pagar"); fix separado no workflow `Dqm3pJo2MNHcRZ1R` (prompt + histórico de conversa). Incrementos futuros (SPEC-004 §7): quiet hours, botões de ação, materializar `ATRASADO`.

- **SPEC-005** (✅ **VALIDADO E2E EM PROD — 2026-06-24**) — **Cérebro da Nina (WhatsApp): memória multi-turno + classificação de conta atrasada**. Causa-raiz: o nó `Nina (OpenRouter)` recebia só a msg atual (sem histórico) → esquecia entre turnos; e `criar_conta` definido como "FUTURA" → conta vencida/em aberto confundia com `pagar_conta` (prova ao vivo: exec 704, o qwen "pensou" *"I don't have the context of the previous question"*).
  - **Fix (workflow `Dqm3pJo2MNHcRZ1R`, draft via MCP, NÃO publicado):** novo nó `Monta Mensagens (memoria)` — ring buffer das últimas 8 trocas por número em staticData, injetado como pares user/assistant no `messages` do qwen; append do histórico no `Interpreta Acao`; reset junto com a sessão no `Filtro`. Prompt do `criar_conta` ampliado p/ contas vencidas/atrasadas em aberto + regra-chave separando de `pagar_conta`.
  - **Harness:** `validate_workflow` valid; 2 auditores **APROVADO** (não-regressão byte-level: 40/43 nós idênticos, UTF-8 do regex sim/não íntegro — o `�` era só o console Windows; memória keyed por número/cap 8/expira c/ sessão; classificação ok; guardrails+de-branding intactos). Docs: `.ai/SPECS/SPEC-005`, premortem, `.ai/HARNESS_RESULTS/SPRINT-7`.
  - **✅ E2E em prod (2026-06-24):** Tiago republicou; teste WhatsApp multi-turno — "conta de luz venceu dia 18" → "80 reais" → a Nina **LEMBROU** (exec `729`: o `messages` levou o turno 1 user+assistant) e emitiu `criar_conta {A_PAGAR, "Conta de luz", 8000, venc 2026-06-18}` (não `pagar_conta`). Memória + classificação confirmadas pelos logs. **Incremento futuro (SPEC-005 §2):** persistência durável via backend `Contexto`/`Sessao` + endpoints (unifica voz do app, hoje também stateless).
  - **Limpar (Tiago):** 2 contas de teste em aberto vão alertar no cron de amanhã 8h — água R$155 (venc 20/04) + luz R$80 (venc 18/06); excluir depois do teste real do cron.
  - ✅ **Modelo:** nó da Nina roda `qwen/qwen3.7-plus` (mais barato e dá conta — **decisão do Tiago 2026-06-24**; docs CLAUDE/STATE alinhadas; ADR-002/MASTERPLAN ficam como registro histórico do plano original `max`).

- **SPEC-006** (✅ **VALIDADO E2E EM PROD — 2026-06-24**) — **Memória conversacional durável (Contexto/Sessao + endpoints)**. Liga os models antes órfãos: `ContextoModule` dentro do `NinaModule` — `ContextoService.historico(limite=8)`/`registrar(role,conteudo)` + `ContextoRepository` (sessão ativa por tenant, **janela deslizante 30min**, get-or-create; **sem migração** — tabelas já existem). Endpoints `GET/POST /api/v1/nina/contexto` tenant-scoped. `OpenRouterAdapter.intent` ganhou `historico?` (injeta pares user/assistant no `messages`). **Voz do app** (`nina.service.processar`) agora carrega o histórico antes do LLM e grava depois (best-effort) → **multi-turno durável** (antes era stateless).
  - **Harness:** `tsc` verde; **18 testes** (sessão/expiração 30min, historico ordem+limite, mapa de role, guarda de tenant, adapter com/sem histórico, processar multi-turno + best-effort). 2 auditores **APROVADO** (1 MAIOR corrigido: `updateMany` com `tenantId` no where — defesa 2 camadas ADR-001). Docs: `.ai/SPECS/SPEC-006`, premortem, `.ai/HARNESS_RESULTS/SPRINT-8`.
  - **✅ E2E em prod (2026-06-24):** orb (voz do app), teste financeiro multi-turno — turno 1 "tenho conta de internet pra pagar" → turno 2 "cento e vinte reais" → criou "conta de internet R$120" **lembrando do turno 1** (Tiago confirmou os 2 turnos). Memória durável OK na voz do app (além do WhatsApp).
  - **🐛 Bug achado (data off-by-one no orb):** "dia 30" virou 29/07. Causa provável: `nina.service` passa `new Date().toISOString()` (UTC) rotulado como "America/Sao_Paulo" no prompt do `OpenRouterAdapter`, e o gemini-flash-lite emite vencimento meia-noite UTC → exibe -1 dia no fuso BR. O WhatsApp (n8n) passa SP-local com `-03:00` e acerta (visto em exec 729: `2026-06-18T00:00:00-03:00`). **Candidato a SPEC-007** (alinhar data SP-local + offset no brain do app; conferir display do front). Também: brain do app chuta default de data/hora em vez de perguntar (reunião 9h, conta 29/07) — ajuste opcional de prompt.
  - **Incrementos futuros:** migrar o cérebro WhatsApp (n8n) do staticData p/ estes endpoints; FK real `Contexto→Sessao`; retention/poda.

- **SPEC-007** (✅ backend pronto + auditado; **pendente deploy do Tiago**) — **Data/timezone do orb (off-by-one) + perguntar data/hora**. Bug: o cérebro do app (`nina.service`) passava `new Date().toISOString()` (UTC) rotulado como SP → o gemini emitia vencimento meia-noite UTC → exibia **-1 dia** ("dia 30"→29/07). Fix: util `common/datas/datas-br.util` (`agoraSaoPaulo()` SP-local `-03:00`; `ancorarDataBR()` ancora datas do LLM ao fuso BR); `nina.service` usa SP-local, ancora vencimento/inicio/fim/dataHora/prazo, e **pergunta** quando falta data/hora (não chuta); prompt reforça `-03:00` + perguntar; exibição com `timeZone: America/Sao_Paulo` explícito (achado dos auditores).
  - **Harness:** `tsc` verde; **84 testes** (13 novos). 2 auditores **APROVADO** (corrigidos: BLOQUEADOR `toLocaleDateString` sem tz; MAIOR fallback UTC→`agoraSaoPaulo`). Docs: `.ai/SPECS/SPEC-007`, premortem, `.ai/HARNESS_RESULTS/SPRINT-9`.
  - **Pendências (Tiago):** **deploy da API** → validar no orb ("dia 30"→30/07; "marca reunião"→pergunta o horário). **Nota:** contas de teste já gravadas em UTC seguem -1 até regravar (dado legado). WhatsApp (n8n) inalterado (já acertava).

- **Premortem do sistema (2026-06-24):** varredura multi-agente (39 agentes, 8 dimensões, 77 achados, 28 confirmados) → **25 riscos priorizados** em `.ai/PREMORTEMS/PREMORTEM-sistema-2026-06-24.md`. Camada 1 (tenant) sólida; maioria dos altos é **latente** (dispara no 2º tenant/incidente). **Release-blockers:** #1 webhook n8n sem HMAC (crítico, live-exploitable), #2 Postgres sem backup, #3 resiliência (sem timeout → silêncio em prod, 6 erros/7 dias). **Multi-tenancy blockers:** RLS camada 2 inerte, tenant hardcoded, login sem tenant. Plano em ondas.
- **SPEC-008** (✅ backend pronto + auditado; **pendente deploy + Onda 1b**) — **Onda 1 do premortem: hardening backend (resiliência + rate-limit)**. (a) **#3 resiliência:** `common/http/fetch-timeout`; adapters OpenRouter/ElevenLabs com timeout (env `LLM_TIMEOUT_MS`=15s, `TTS_TIMEOUT_MS`=12s) → timeout/erro vira 503 (não pendura ~5min). (b) **#7 rate-limit:** `@nestjs/throttler` global + `TenantThrottlerGuard` (chave por **tenant**, não pelo IP único do n8n) + `/auth/login` 5/min + health isento.
  - **Harness:** `tsc` verde; **92 testes** (8 novos). 2 auditores **APROVADO** — adversarial **provou empiricamente** o keying por tenant (Nest real, 2 tenants/mesmo IP → baldes separados); MAIOR corrigido (timeouts → env). Docs: `.ai/SPECS/SPEC-008`, premortem, `.ai/HARNESS_RESULTS/SPRINT-10`.
  - **Pendências (Tiago):** **deploy**. **Onda 1b:** #3-n8n (timeout/retry/onError + Error Workflow via MCP + publish), **#1 webhook HMAC** (nó + config Evolution), **#2 backup** (pg_dump + EasyPanel). **Ondas 2–3:** lembrete recorrente (#6), tz financeiro (#8), idempotência (#9), observabilidade (#14), RLS camada 2 + multi-tenant (#4/#5/#11).

- **SPEC-009** (✅ código/draft pronto + auditado; **pendente handoff do Tiago**) — **Onda 1b: backup + resiliência n8n + plano webhook**. (a) **#2 backup:** `infra/backup/` (`backup.sh` pg_dump→gzip→retenção→off-site tolerante a falha; `restore.sh`; README com agendamento EasyPanel + RPO/RTO + **testar restore**). (b) **#3-n8n:** `options.timeout=15000` nos 23 httpRequest do brain (draft via MCP, drift-zero, **NÃO publicado**). ⚠️ **timeout só corta o pendura de ~5min; o anti-silêncio (onError+fallback+Error Workflow) precisa dos passos de UI** — o MCP não seta `retryOnFail`/`onError`. (c) **#1 webhook:** plano fail-open→fail-closed documentado (validação de segredo + path não-advinhável; **a ordem importa**).
  - **Harness:** `bash -n` ok; `validate_workflow` valid; 2 auditores **APROVADO** (corrigidos: off-site tolerante a falha; guarda no restore; caveat honesto + passos de UI no handoff). Docs: `.ai/SPECS/SPEC-009`, premortem, `.ai/HARNESS_RESULTS/SPRINT-11`.
  - **Pendências (Tiago) — handoff (SPEC-009 §5):** **backup** (agendar + volume persistente + **testar restore**); **n8n** (UI: retry/onError/fallback + Error Workflow + publish); **webhook #1** (✅ **FECHADO E VERIFICADO E2E — Claude 2026-06-25, SPRINT-12, v8536d345**: nó `Valida Segredo (webhook)`, segredo via **query-param `?token=`** (Evolution 2.3.7 sem header), env no **serviço n8n** + `N8N_BLOCK_ENV_ACCESS_IN_NODE=false`; forjado bloqueado (exec 827/828) + real passa. Doc: `n8n/workflows/nina-webhook-seguranca.md`). **Resto do premortem:** Ondas 2–3 (#6 lembrete recorrente, #8 tz financeiro, #9 idempotência, #14 observabilidade, #4/#5/#11 RLS+multi-tenant).

## Decisões (ADR)
ADR-001 multi-tenant/RLS · ADR-002 modelos OpenRouter · ADR-003 gatilho · ADR-004 n8n-via-API · ADR-005 recorrência agenda · ADR-006 RLS camada 2 (role não-owner) · **ADR-007 modelagem financeira** (Conta unificada `origem` + Categoria tenant-scoped + regime de caixa + confirmação desacoplada do canal).
